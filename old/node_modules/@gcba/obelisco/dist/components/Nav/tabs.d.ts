import React from 'react';
import { NavItem, NavItemComponentProps, NavProps } from '.';
export interface TabsItem extends NavItem {
    iconTabs: string;
    content: React.ReactNode;
}
export interface TabsProps extends NavProps {
    items: TabsItem[];
    classUl: string;
    idPrefix: string;
    label: string;
    isWithButton?: boolean;
}
export interface TabsComponentsProps extends NavItemComponentProps, TabsItem {
}
export declare const NavTabsSlider: React.FC<TabsProps>;
