import * as React from 'react';
import { ButtonType, Size } from '../utils';
export interface ButtonProps {
    type: ButtonType;
    size?: Size;
    isBlock?: boolean;
    isDisabled?: boolean;
    className?: string;
    isOutline?: boolean;
    isIconSpinner?: React.ReactNode;
    ariaLabel?: string;
}
export declare const Button: React.FC<ButtonProps>;
