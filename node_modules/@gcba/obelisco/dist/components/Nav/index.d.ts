import * as React from 'react';
import { Size } from '../utils';
export interface NavItem {
    name: string;
    id: string;
    type?: Size;
    href?: string;
    disabled?: boolean;
    children?: NavItem[];
    hasBordered?: boolean;
}
export interface NavProps {
    items: NavItem[];
    selected?: string;
    navSize?: 'default' | 'large';
    onClick?: (id: string) => void;
    hasIcon?: boolean;
}
export declare const Nav: React.FC<NavProps>;
export declare const NavHorizontal: React.FC<NavProps>;
